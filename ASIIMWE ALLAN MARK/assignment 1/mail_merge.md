The program enables a user to automatically send a similar email to various users provided he has their details, such as names.
It works by;
it uses the time_t now function to acquire the current time,
the we format the time we have obtained into the desired format
the program then gets the different desired inputs from the user such as name, program and year of study
It then combines all of this information to create a whole email to send out.
